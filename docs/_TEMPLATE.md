# توثيق: [اسم الصفحة/المكون]

> أُنشئ بتاريخ: YYYY-MM-DD | آخر تحديث: YYYY-MM-DD

---

## الملفات المُنشأة

```
frontend/app/[locale]/PAGE/page.tsx
frontend/components/SECTION/ComponentName.tsx
```

## المكونات

| المكون          | الملف                                  | الوصف     |
| --------------- | -------------------------------------- | --------- |
| `ComponentName` | `components/section/ComponentName.tsx` | وصف مختصر |

## Props والـ Types المستخدمة

```typescript
// من types.ts
interface ExampleType { ... }
```

## مصدر البيانات الحالي

- **الآن:** `mock-data.ts` — دالة `getMockXxx()`
- **لاحقاً:** `strapi.ts` — دالة `getXxxFromStrapi()`

## ملاحظات التنفيذ

- أي قرارات مهمة اتُّخذت أثناء البناء
- مشاكل واجهتها وكيف حُلّت

## TODO المتبقي لهذه الصفحة

- [ ] مهمة مؤجلة إن وجدت
